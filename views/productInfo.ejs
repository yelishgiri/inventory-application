<h4><%= product.product_name %></h4>
<img src="<%= product.product_image_url %>" alt="product_image" />
<ul>
    <li>
        Description: <%= product.product_description %>
    </li>
    <li>
        Category: <%= product.category_name %>
    </li>
    <li>
        Dated Added: <%= product.product_created_at %>
    </li>
    <a href="/product/id/<%= product.product_id %>/updateProduct">Update</a>
    <form action="/product/id/<%= product.product_id %>/deleteProduct" method="POST" style="display: inline;">
        <button type="submit" onclick="return confirm('Are you sure you want to delete this product?');">Delete</button>
    </form>
</ul>
